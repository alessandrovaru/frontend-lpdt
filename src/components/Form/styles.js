import styled from "styled-components";

export const NewCourseCard = styled.div`
  box-shadow: 0 0 1rem 0 rgba(0, 0, 0, 0.2);
  background-color: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(5px) !important;
  border-radius: 5px;
  width: 600px;
  padding: 40px;
`;
